header.header
  .header-inner
    h1#js-global-title.header-title #{data.siteWide.title}
    ul.header-menu
      .header-menu-inner
        each item in data.siteWide.menu
          li.header-menus(class=`header-menus-${item.name}`)
            a.header-link(href=`${item.link}`) #{item.name}
